name: Deploy Supabase Edge Functions

on:
  push:
    branches:
      - main
    paths:
      - "supabase/functions/**"
      - "supabase/config.toml"
      - "src/lib/big-official.ts"
      - "src/lib/big-carryover.ts"
      - ".github/workflows/deploy-supabase-functions.yml"
  workflow_dispatch:

jobs:
  deploy:
    runs-on: ubuntu-latest
    env:
      FORCE_JAVASCRIPT_ACTIONS_TO_NODE24: true
      PROJECT_ID: jtypbwgdtqeznhxffpgo
      SUPABASE_ACCESS_TOKEN: ${{ secrets.SUPABASE_ACCESS_TOKEN }}

    steps:
      - name: Checkout
        uses: actions/checkout@v6

      - name: Ensure Supabase token exists
        run: |
          if [ -z "$SUPABASE_ACCESS_TOKEN" ]; then
            echo "Missing SUPABASE_ACCESS_TOKEN GitHub Actions secret."
            echo "Add a fresh Supabase Personal Access Token in Settings > Secrets and variables > Actions."
            exit 1
          fi

      - name: Setup Supabase CLI
        uses: supabase/setup-cli@v1
        with:
          version: latest

      - name: Deploy Edge Functions
        run: |
          set +e
          supabase functions deploy --project-ref "$PROJECT_ID" --use-api
          deploy_status=$?
          set -e

          if [ "$deploy_status" -eq 0 ]; then
            exit 0
          fi

          echo "::warning::Management API deploy failed; retrying with the default Supabase deploy gateway."
          supabase functions deploy --project-ref "$PROJECT_ID"
